import type { StatsCardItem } from "~/components/domain/dashboard/StatsCard/stats-card.types";

type StatsHorizontalBarChartProps = {
  /** Stats card items to display as horizontal bars */
  items: StatsCardItem[];
};

export type { StatsHorizontalBarChartProps };